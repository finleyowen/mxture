extends ../template.pug

block headcontent
    - var title = profile.username

block bodycontent
    small Profile
    h1= profile.display_name != '' ? profile.display_name : profile.username
        if profile.descriptors
            small  (#{profile.descriptors})
    a(href=profile.website) #{profile.website}
    p= profile.bio